# Standalone profile — boots catalog with no external infrastructure (H2 only).
# Spring Cloud Config is disabled via run args (see CatalogStandaloneApplication javadoc):
#   --spring.cloud.config.enabled=false --spring.config.import=

spring.datasource.url=jdbc:h2:mem:catalogdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=false
server.port=8080
