# Standalone profile: make the config-server import optional so the service boots
# even when no Spring Cloud Config server is running. Production bootstrap.yml is unchanged.
spring:
  config:
    import: "optional:configserver:http://localhost:8071"
  cloud:
    config:
      enabled: false
